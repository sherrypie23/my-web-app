version: 2.1

jobs:
  build:
    working_directory: ~/repo
    docker:
      - image: cimg/node:18.10.0
    steps:
      - checkout
      # Éventuellement, exécutez des étapes pour installer les dépendances de votre projet

  sonarcloud_scan:
    docker:
      - image: sonarsource/sonar-scanner-cli:latest
    steps:
      - checkout
      - run:
          name: SonarCloud Scan
          command: sonar-scanner \
            --define sonar.organization=sherrypie23 \
            --define sonar.projectKey=sherrypie23_my-web-app \
            --define sonar.sources=. \
            --define sonar.host.url=https://sonarcloud.io \
            --define sonar.login=$SONAR_TOKEN

  deploy:
    machine:
      enabled: true
    steps:
      - run:
          name: Deploy Over SSH
          command: |
            ssh azureuser@172.211.132.94 "cd /opt/lampp/htdocs/mutillidae && sudo /opt/lampp/xampp restartapache"

orbs:
  sonarcloud: sonarsource/sonarcloud@2.0.0

workflows:
  version: 2
  main:
    jobs:
      - build
      - sonarcloud_scan:
          requires:
            - build
      - deploy:
          requires:
            - sonarcloud_scan # Exécuter la tâche de déploiement après l'analyse SonarCloud
            - build
          filters:
            branches:
              only: main
